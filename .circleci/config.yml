version: 2.1

orbs:
  path-filtering: circleci/path-filtering@0.1.3
workflows:
  generate-config:
    jobs:
      - path-filtering/filter:
          base-revision: master
          configuration-path: .circleci/continue_config.yml
          mapping: |
            src/.* build-code true
            eks/.* build-eks true




